针对您在 **2026 年** 开展的小麦（Wheat）强化学习研究，结合 **Gym-DSSAT**（底层基于 DSSAT 4.8+）和 **天授（Tianshou）** 框架，我为您设计了如下的技术方案。

---

## 一、 软件架构与版本兼容性方案

在 2026 年的环境下，保持“Gymnasium 协议”的一致性是避坑的关键。

### 1. 核心软件栈

* **操作系统**: **Ubuntu 22.04/24.04 LTS** (强烈建议)。因为 Gym-DSSAT 依赖的 PDI (Parallel Data Interface) 在 Linux 下运行最稳定，且天授的多进程采样在 Linux 上效率更高。
* **Python 版本**: **Python 3.10 - 3.12**。建议使用 `Conda` 环境进行隔离。
* **核心库版本建议**:
* **DSSAT**: v4.8.5 (虽然dssat已经发布到v4.8.5，但是gym-dssat项目应该仍然用的是v4.7.5，我们的目的是早日将我们的项目跑起来，验证我们的方法论，所以可以接受使用旧版)。
* **gym-dssat-pdi**: 建议检查是否有 2025 年后的更新。由于 OpenAI `gym` 已停止维护，请优先寻找支持 **Gymnasium** 接口的分支。
* **Tianshou (天授)**: v1.0.0+ (2026年的版本已全面拥抱 Gymnasium)。
* **PyTorch**: v2.4+ (支持最新的 CUDA 加速)。



### 2. 兼容性关键提醒

> [!CAUTION]
> **Gym vs. Gymnasium**: 如果您的 Gym-DSSAT 依然是老旧的 `import gym` 版本，而天授需要 `import gymnasium`，您必须使用 `shimmy` 库进行包装：
> `env = shimmy.GymV21CompatibilityV0(env)`

---

## 二、 方案框架设计

整个研究流程可划分为以下四个阶段：

### 1. 环境构建阶段 (Wheat Environment)

* **小麦模型选择**: DSSAT 支持 `CERES-Wheat` 和 `Nwheat`。建议使用 **Nwheat**，因为它在模拟氮素转化和分配方面更精细。
* **小麦实验文件配置**:
* 在 `gym-dssat-pdi` 的数据目录下，创建一个针对小麦的实验文件夹（如 `WHEAT_TEST`）。
* 准备相关的 `.WTH` (气象)、`.SOL` (土壤)、`.CUL` (品种参数) 和 `.SNX` (实验方案) 文件。（可以用gym-dssat项目中的案例，或者使用DSSAT项目在github上公开的data文件等。）


* **动作空间 (Action Space)**:
* 离散型：如 `0: 不操作`, `1: 施肥 50kg`, `2: 灌溉 20mm`。（你可以根据需要和农业常识自行设计）
* 连续型：天授对连续动作（如 SAC, PPO）支持极好，可以直接输出具体的氮肥量 。



### 2. 天授智能体搭建阶段 (Agent Design)

天授的核心流程如下：

* **Vectorized Environment**: 利用 `tianshou.env.SubprocVectorEnv` 同时开启 16-32 个小麦模拟环境（根据CPU核心数决策并行数量）。
* **Policy 选择**:
* 如果动作是离散的：使用 **Rainbow DQN**。
* 如果动作是连续的（如精确水肥）：首选 **PPO** 或 **SAC**。


* **Collector & Buffer**: 使用 `VectorReplayBuffer` 来存储数万天的作物生长数据。

### 3. 奖励函数设计 (Reward Engineering)

针对小麦水氮优化，建议采用多维度加权奖励：
你可以查阅、参考相关文献，并根据农业常识、小麦种植常识来设计奖励函数。


*注意：由于产量奖励只在最后一天产生（稀疏奖励），建议在生长过程中加入“生长潜力”奖励，如每天 LAI 的增量。*

---

## 三、 实施步骤指南

1. **第一步：编译 DSSAT 引擎**。确保 DSSAT 的 Fortran 源码在你的 Linux 环境下编译成功，并配置好环境变量。
2. **第二步：注册 Gym 环境**。
```python
import gymnasium as gym
from gym_dssat_pdi.envs import DssatEnv

# 指向你的小麦实验文件
env_args = { "dssat_exe": "/path/to/dscsm048", "fileX": "/path/to/WHEAT.WQX" }
env = DssatEnv(**env_args)

```


3. **第三步：天授 Pipeline**。
```python
import tianshou as ts
# 1. 定义网络
# 2. 定义策略 (Policy)
# 3. 定义数据采集器 (Collector)
# 4. 执行训练 (Trainer)

```



---

## 四、 专家提醒：您可能忽略的维度

### 1. “死亡日期”的问题

小麦在极端水分或氮素胁迫下可能提前停止发育（枯死）。如果智能体在训练前期总是导致小麦在第 50 天就“挂掉”，它将无法学到收敛后的产量。

* **对策**: 在 `Observation Space` 中加入 `istage` (生长阶段) 标志。如果作物过早死亡，给予一个巨大的负惩罚值，并强制结束该回合。

### 2. 状态特征增强 (Observation Expansion)

单纯的 DSSAT 输出可能不足。

* **建议**: 为后续进行数据同化（测量得到的LAI、土壤水分等数据）设计合适的接口。

### 3. 气象不确定性 (Weather Uncertainty)

如果您只用一年的气象数据训练，智能体会陷入“过拟合”，学到一个只能在特定年份生效的策略。

* **思路**: 利用天授的并行能力，让不同的 `SubprocVectorEnv` 加载不同年份（干旱年、丰收年、多雨年）的 `.WTH` 文件进行**联合训练 (Joint Training)**。（不同年份的数据可以暂时用gym-dssat或DSSAT官方示例文件中的内容）

### 4. 离线预训练 (Offline RL)

如果您手头已经有大量的小麦实验数据。

* **思路**: 不要直接开始随机探索。先用天授的 **Decision Transformer (DT)** 在现有数据（gym-dssat或DSSAT官方的示例文件）上跑一遍，给智能体建立一个“常识性”的基础，再进行在线强化学习，效率会提升 5-10 倍。

我希望未来用这个项目来验证一些最新的强化学习方法、理论在农业领域灌溉决策RL的适用性。