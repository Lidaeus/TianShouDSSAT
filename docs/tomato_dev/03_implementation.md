# 第三章：核心技术实现细节

## 3.1 跨作物变量标准化
通过 PDI YAML 修改，实现了语义层映射（如将番茄的 SDWT 映射为统一的 grnwt），并为不支持 istage 的作物注入虚拟阶段值，维持张量维度一致。

## 3.2 资源全路径自动探测
实现了智能代理拷贝机制，自动识别并拷贝所需的 .WTH, .CLI, .SOL 文件至内存临时文件系统，确保物理引擎极速访问。

## 3.3 嵌套环境依赖注入
针对 PDI 插件调用 Python 时找不到依赖的问题，实现了动态 PYTHONPATH 注入，确保 ZMQ 握手脚本在正确的虚拟环境下执行。
